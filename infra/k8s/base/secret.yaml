# Real deployments source these from a SecretsManager / Vault.
# This stub exists so `kubectl apply` succeeds in dev clusters; replace before any
# environment that handles real traffic.
apiVersion: v1
kind: Secret
metadata:
  name: aegisflow-secrets
type: Opaque
stringData:
  POSTGRES_PASSWORD: devonly_change_me
  JWT_SECRET: devonly_change_me
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
